# scripts/.env.example — Template for LOCAL TESTING ONLY
# IMPORTANT: Do NOT store real credentials here.
# In CI/CD, set these values as GitHub Secrets (ZILLOW_EMAIL, ZILLOW_PASSWORD).
# This file is safe to commit; the real .env file is excluded by .gitignore.

# Zillow account email (used to log in to zillow.com/research/data/)
ZILLOW_EMAIL=your_zillow_email@example.com

# Zillow account password
ZILLOW_PASSWORD=your_zillow_password
