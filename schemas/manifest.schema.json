{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "manifest.schema.json",
  "title": "Data Manifest",
  "description": "Schema for data/manifest.json — auto-generated index of all data files with a freshness timestamp.",
  "type": "object",
  "required": ["generated", "files"],
  "additionalProperties": false,
  "properties": {
    "generated": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC timestamp of when this manifest was last regenerated."
    },
    "files": {
      "type": "array",
      "description": "List of relative data file paths included in the build.",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1
    },
    "file_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Total count of files listed (may be absent in older manifests)."
    }
  }
}
