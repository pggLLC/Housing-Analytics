// js/config.js.template
// Copy to js/config.js (DO NOT commit config.js) and add your keys if you want live calls.
// Recommended: rely on /data/fred-data.json generated by GitHub Actions and keep FRED key server-side (as a secret).

window.APP_CONFIG = {
  CENSUS_API_KEY: "",
  FRED_API_KEY: ""
};
