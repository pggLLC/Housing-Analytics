# Example .env file — copy this to .env and fill in your values
# NEVER commit your real .env file to the repository.

# ── Email credentials (store as GitHub Secrets, not here) ──────────────────────
# EMAIL_USER=your-gmail-address@gmail.com
# EMAIL_PASSWORD=your-gmail-app-password

# ── Website to monitor ─────────────────────────────────────────────────────────
# WEBSITE_URL=https://your-website.com

# ── Monitoring report recipient ────────────────────────────────────────────────
# Monitoring reports will be sent to this address.
RECIPIENT_EMAIL=communityplanner@gmail.com

# ── GitHub Secrets setup ───────────────────────────────────────────────────────
# Add the following secrets in your repository under:
#   Settings → Secrets and variables → Actions → New repository secret
#
#   EMAIL_USER      — Gmail address used to send monitoring emails
#   EMAIL_PASSWORD  — Gmail App Password (not your normal password)
#   WEBSITE_URL     — Full URL of the website to monitor (e.g. https://example.com)
#
# RECIPIENT_EMAIL is not sensitive and is set directly in the workflow file.
# Example .env file - for LOCAL TESTING ONLY
# IMPORTANT: Do NOT store real email addresses or passwords here.
# All sensitive values must be stored in GitHub Secrets for CI/CD.

# Gmail account used to send monitoring emails
EMAIL_USER=your_gmail_account@gmail.com

# Gmail App Password (generate at https://myaccount.google.com/apppasswords)
EMAIL_PASSWORD=your_gmail_app_password

# The website URL to monitor
WEBSITE_URL=https://your-website-url.com

# Recipient email address for monitoring reports
# IMPORTANT: Store the actual address ONLY in GitHub Secrets (RECIPIENT_EMAIL),
# never commit a real email address to this file.
RECIPIENT_EMAIL=your_recipient_email@example.com
